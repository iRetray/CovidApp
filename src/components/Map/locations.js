

const contriesLocations = results.map((data, i) => {
    return (
        <div
            /**lat = {data.} mirar dentro de la API como devuleve longitud y latitud para agregarlos 
            lng = {data.}*/
            style = {{
                color: "red",
                backgroundColor: "#FFF",
                height: "25px",
                widht: "35px",
                textAlign : "center"
                
        }}
        >
            {date.case}
        </div>
    );
});

export default { contriesLocations }